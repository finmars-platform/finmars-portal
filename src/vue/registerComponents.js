import {defineCustomElement} from "vue";
import {FmBtn} from "@finmars/ui";

export default function registerVueComponents () {
    /*
    // Example of creating a WEB component based on the VUE component FmBtn
    const FmBtnCustomElem = defineCustomElement(FmBtn, {shadowRoot: false});

    customElements.define('fm-btn', FmBtnCustomElem);
    */
}